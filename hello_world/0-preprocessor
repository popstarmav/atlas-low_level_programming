#!/bin/bash

# Define the input C file
CFILE="your_c_file.c"

# Define the output file
OUTPUT_FILE="c"

# Run the C file through the preprocessor and save the result
cpp -P "$CFILE" > "$OUTPUT_FILE"

